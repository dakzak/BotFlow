{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'export DATABASE_URL=\"${DATABASE_URL:-file:/data/botflow.db}\" SESSIONS_DIR=\"${SESSIONS_DIR:-/data/sessions}\"; mkdir -p \"$SESSIONS_DIR\"; npx prisma migrate deploy && node server.js'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
